import '../../../core/app_export.dart';/// This class is used in the [eventlist_item_widget] screen.
class EventlistItemModel {EventlistItemModel({this.groupBy, this.id, }) { groupBy = groupBy  ?? "";id = id  ?? ""; }

String? groupBy;

String? id;

 }
